# ===========================================================================
# Deliverable 4 — SLM fine-tuning extras (PEFT / QLoRA)
# ===========================================================================
# Install on top of requirements.txt when you want to train or run the tuned
# small language model:
#     pip install -r requirements.txt -r requirements-slm.txt
#
# torch + transformers already come in via sentence-transformers in the core
# file; they are listed here too for clarity / standalone use.

torch>=2.2
transformers>=4.40
peft>=0.10
accelerate>=0.29
datasets>=2.18

# bitsandbytes enables 4-bit QLoRA loading.  It requires a CUDA GPU; on CPU-only
# machines the trainer/generator transparently fall back to plain LoRA / fp32,
# so this line is optional.  Uncomment on a GPU box:
# bitsandbytes>=0.43 ; platform_system == "Linux"

# TRL's SFTTrainer is the production training alternative to the self-contained
# loop in train_slm.py.  Optional.
# trl>=0.8

# RAGAS gives LLM-judged faithfulness / answer-relevancy for the D3/D4 eval.
# Needs an evaluator LLM (e.g. OPENAI_API_KEY).  Optional — the eval scripts
# fall back to a deterministic lexical scorer without it.
# ragas>=0.1
