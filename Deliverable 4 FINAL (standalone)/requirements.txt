# ===========================================================================
# PDF-Papers AI Agent — core requirements (Deliverables 1-3 + D4 runtime)
# ===========================================================================
# Heavy SLM fine-tuning extras (peft/bitsandbytes/trl/datasets/ragas) live in
# requirements-slm.txt so the core agent installs fast on a laptop.

# --- D1: streaming learner + AutoML ---------------------------------------
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.4,<2.0
matplotlib>=3.8,<4.0
optuna>=3.6,<4.0
river>=0.21,<1.0

# --- D2: retrieval stack + API --------------------------------------------
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
pymongo>=4.6
qdrant-client>=1.9,<1.11        # keep within 1 minor of the v1.9.2 server image (docker-compose)
sentence-transformers>=3.0        # also pulls torch + transformers (used by D4 SLM)
rank-bm25>=0.2.2
pymupdf>=1.24
neo4j>=5.18
tqdm>=4.66
requests>=2.31

# --- corpus / abstract extraction -----------------------------------------
pdfplumber>=0.11

# --- tests ----------------------------------------------------------------
pytest>=8.0,<9.0
