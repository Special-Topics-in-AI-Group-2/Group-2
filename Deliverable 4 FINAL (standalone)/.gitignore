# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
venv/
.venv/
env/

# Project artifacts (reproducible — regenerated by scripts)
artifacts/
runs/
ablation_results/
*.log
feedback_log.jsonl
d3_eval_results.json
d3_ragas_results.json

# Data: keep the metadata + scripts, not the binary PDFs (re-download with
# scripts/download_corpus.py).  Comment these out to commit the corpus.
data/pdfs/*.pdf

# Model / HF caches
.cache/
hf_cache/

# OS
.DS_Store
Thumbs.db
.env
